'use client';
import {useState} from 'react';import {PROFILE} from '../../lib/data';
export default function Contact(){const[sent,setSent]=useState(false);const[form,setForm]=useState({name:'',email:'',message:''});return <main><section><div className="container"><span className="pill">CONTACT</span><h1 style={{marginTop:18}}>Let's talk endpoint engineering.</h1><div className="grid two" style={{marginTop:32}}><div><p className="muted">Current focus: Senior Endpoint Engineer, Intune Architect and Platform Engineer opportunities.</p><div className="card"><p><span className="muted">Email</span><br/>{PROFILE.email}</p><p><span className="muted">LinkedIn</span><br/><a className="accent" href={PROFILE.linkedin}>linkedin.com/in/sumanpainuly</a></p></div></div><form className="card" onSubmit={e=>{e.preventDefault();setSent(true)}}><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} style={input}/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} style={input}/></label><label>Message<textarea required rows={6} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} style={input}/></label><button className="btn" type="submit">{sent?'Message captured ✓':'Send inquiry →'}</button>{sent&&<p className="accent">Thanks — your message passed client-side validation. Connect by email for production contact handling.</p>}</form></div></div></section></main>}
const input={display:'block',width:'100%',margin:'8px 0 18px',padding:'12px',borderRadius:8,border:'1px solid var(--border)',background:'#f7f1e7',color:'var(--text)',outline:'none'};