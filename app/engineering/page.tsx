'use client';
import {useState} from 'react';import ProjectCard from '../../components/ProjectCard';import {PROJECTS} from '../../lib/data';
export default function Engineering(){const[filter,setFilter]=useState('All');const domains=['All',...Array.from(new Set(PROJECTS.map(p=>p.domain)))];const list=filter==='All'?PROJECTS:PROJECTS.filter(p=>p.domain===filter);return <main><section><div className="container"><span className="pill">ENGINEERING</span><h1 style={{marginTop:18}}>Systems that remove manual work.</h1><p className="muted">Case studies across automation, migration, macOS/JAMF and security.</p><div className="tags" style={{margin:'28px 0'}}>{domains.map(d=><button className="btn" key={d} onClick={()=>setFilter(d)} style={{opacity:filter===d?1:.6}}>{d}</button>)}</div><div className="grid two">{list.map(p=><ProjectCard key={p.slug} project={p}/>)}</div></div></section></main>}