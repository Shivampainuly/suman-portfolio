'use client';
import {SKILLS} from '../../lib/data';import {Radar,RadarChart,PolarGrid,PolarAngleAxis,PolarRadiusAxis,ResponsiveContainer} from 'recharts';
export default function Skills(){const data=Object.entries(SKILLS).map(([subject,current])=>({subject,current,target:95}));return <main><section><div className="container"><span className="pill">SKILL MATRIX</span><h1 style={{marginTop:18}}>Endpoint depth, not keyword breadth.</h1><div className="card" style={{height:460,marginTop:30}}><ResponsiveContainer width="100%" height="100%"><RadarChart data={data}><PolarGrid stroke="#c8b9a4"/><PolarAngleAxis dataKey="subject" stroke="#6f675c"/><PolarRadiusAxis domain={[0,100]} stroke="#a8957d"/><Radar name="Current" dataKey="current" stroke="#355c4a" fill="#355c4a" fillOpacity={.2}/><Radar name="Target" dataKey="target" stroke="#9a7653" fill="#9a7653" fillOpacity={.08}/></RadarChart></ResponsiveContainer></div><div className="grid three" style={{marginTop:20}}>{Object.entries(SKILLS).map(([k,v])=><div className="card" key={k}><span className="muted">{k}</span><h2 className="accent" style={{marginTop:8}}>{v}%</h2></div>)}</div></div></section></main>}