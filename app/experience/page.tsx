'use client';
import {useState} from 'react';import {EXPERIENCE} from '../../lib/data';
export default function Experience(){const[open,setOpen]=useState(0);return <main><section><div className="container"><span className="pill">EXPERIENCE</span><h1 style={{marginTop:18}}>Timeline of impact.</h1><p className="muted">Endpoint operations, escalation ownership and automation across Windows and macOS.</p><div className="grid" style={{marginTop:32}}>{EXPERIENCE.map((r,i)=><div className="card" key={r.company}><button onClick={()=>setOpen(open===i?-1:i)} style={{background:'transparent',border:0,color:'inherit',width:'100%',textAlign:'left',cursor:'pointer'}}><div style={{display:'flex',justifyContent:'space-between',gap:20}}><div><span className="accent mono">{r.period}</span><h2 style={{marginTop:10}}>{r.role}</h2><p className="muted">{r.company}</p></div><span className="pill">{open===i?'COLLAPSE':'EXPAND'}</span></div></button>{open===i&&<div style={{marginTop:24}}><div className="tags">{r.stack.map(s=><span className="tag" key={s}>{s}</span>)}</div><ul>{r.highlights.map(h=><li key={h} style={{margin:'14px 0',lineHeight:1.6}}>{h}</li>)}</ul></div>}</div>)}</div></div></section></main>}