'use client';
import {useState} from 'react';
import {ArrowUpRight,X} from 'lucide-react';
import {PROJECTS} from '../lib/data';
type Project=typeof PROJECTS[number];
export default function ProjectCard({project}:{project:Project}){const[open,setOpen]=useState(false);return <><button onClick={()=>setOpen(true)} className="card" style={{textAlign:'left',cursor:'pointer',width:'100%'}}><div style={{display:'flex',justifyContent:'space-between'}}><span className="pill">{project.domain}</span><ArrowUpRight className="accent"/></div><h3 style={{marginTop:22}}>{project.title}</h3><p className="muted">{project.problem}</p><div className="tags">{project.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div></button>{open&&<div onClick={()=>setOpen(false)} style={{position:'fixed',inset:0,zIndex:90,background:'rgba(41,37,31,.72)',padding:'5vh 20px',overflow:'auto'}}><div className="card" onClick={e=>e.stopPropagation()} style={{maxWidth:820,margin:'auto'}}><div style={{display:'flex',justifyContent:'space-between'}}><div><span className="pill">{project.domain}</span><h2 style={{marginTop:16}}>{project.title}</h2></div><button onClick={()=>setOpen(false)} aria-label="Close"><X/></button></div><div className="grid three" style={{marginTop:28}}><div><span className="amber mono">01 / PROBLEM</span><p>{project.problem}</p></div><div><span className="amber mono">02 / SOLUTION</span><p>{project.solution}</p></div><div><span className="amber mono">03 / RESULT</span><p className="accent">{project.result}</p></div></div></div></div>}</>}