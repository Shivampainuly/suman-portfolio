'use client';
import {useEffect,useState} from 'react';
import {Search,X} from 'lucide-react';
import Link from 'next/link';
const items=[['Home','/'],['Experience','/experience'],['Engineering','/engineering'],['Skills','/skills'],['Lab','/lab'],['Contact','/contact']];
export function CommandPalette(){const[open,setOpen]=useState(false);const[q,setQ]=useState('');useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)}if(e.key==='Escape')setOpen(false)};window.addEventListener('keydown',f);return()=>window.removeEventListener('keydown',f)},[]);return <><button className="btn mono" onClick={()=>setOpen(true)} aria-label="Open command palette"><Search size={15}/>⌘K</button>{open&&<div style={{position:'fixed',inset:0,zIndex:100,background:'rgba(41,37,31,.72)',padding:'10vh 20px'}} onClick={()=>setOpen(false)}><div className="card" style={{maxWidth:620,margin:'auto'}} onClick={e=>e.stopPropagation()}><div style={{display:'flex',gap:10}}><Search/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search pages..." style={{flex:1,fontSize:18}}/><button onClick={()=>setOpen(false)} aria-label="Close"><X/></button></div><div style={{marginTop:18}}>{items.filter(x=>x[0].toLowerCase().includes(q.toLowerCase())).map(x=><Link key={x[1]} href={x[1]} onClick={()=>setOpen(false)} style={{display:'block',padding:'12px 8px',borderTop:'1px solid var(--border)'}}>{x[0]}</Link>)}</div></div></div>}</>}